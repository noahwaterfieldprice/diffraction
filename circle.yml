machine:
  python:
    version: 3.5.0

dependencies:
  pre:
    - pip install -e .

test:
    override:
      - py.test --cov=diffraction --cov-report=term-missing --verbose tests
    post:
      - coveralls