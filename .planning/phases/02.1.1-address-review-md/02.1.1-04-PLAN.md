---
phase: 02.1.1-address-review-md
plan: 04
type: execute
wave: 3
depends_on: ["02.1.1-03"]
files_modified:
  - pyproject.toml
  - src/diffraction/py.typed
  - tests/unit/test_lattice.py
  - tests/unit/test_crystal.py
  - tests/conftest.py
autonomous: true
requirements:
  - REVIEW-M4
  - REVIEW-m6
  - REVIEW-m14

must_haves:
  truths:
    - "pyproject.toml has license, authors, classifiers, project.urls, readme"
    - "py.typed marker file exists at src/diffraction/py.typed"
    - "Version in pyproject.toml is '0.2'"
    - "CALCITE_LATTICE_PARAMS is imported from conftest in test files, not redefined"
    - "m6 (get_cif_data overloads) is explicitly not implemented — get_numerical_cif_data is the typed alternative"
  artifacts:
    - path: "pyproject.toml"
      provides: "Complete project metadata"
      contains: "license"
    - path: "src/diffraction/py.typed"
      provides: "PEP 561 typed marker"
    - path: "tests/unit/test_lattice.py"
      provides: "Imports CALCITE_LATTICE_PARAMS from conftest"
    - path: "tests/unit/test_crystal.py"
      provides: "Imports CALCITE_LATTICE_PARAMS from conftest"
  key_links:
    - from: "tests/unit/test_lattice.py"
      to: "tests/conftest.py"
      via: "import CALCITE_LATTICE_PARAMS"
      pattern: "from conftest import"
---

<objective>
Add project metadata to pyproject.toml, create py.typed marker, deduplicate test constants, and close m6 (get_cif_data overloads documented as infeasible).

Purpose: Complete project metadata (M4), establish PEP 561 compliance (py.typed), clean up test constant duplication (m14), and formally close m6. This is the final polish plan for Phase 02.1.1.

Output: Complete pyproject.toml, py.typed marker, clean test imports, all REVIEW.md findings addressed.
</objective>

<execution_context>
@/Users/noah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/noah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1.1-address-review-md/02.1.1-CONTEXT.md
@.planning/phases/02.1.1-address-review-md/02.1.1-03-SUMMARY.md
@pyproject.toml
@tests/conftest.py
@tests/unit/test_lattice.py
@tests/unit/test_crystal.py
@src/diffraction/cif/helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add project metadata, version bump, and py.typed marker</name>
  <files>
    pyproject.toml
    src/diffraction/py.typed
  </files>
  <action>
1. **Update `pyproject.toml` project metadata:**
   - Change `version = "0.1"` to `version = "0.2"`
   - Add `readme = "README.md"` to `[project]`
   - Add `license = {text = "MIT"}` (or appropriate license — check if LICENSE file exists; if not, use MIT per common scientific Python convention)
   - Add authors:
     ```toml
     authors = [
         {name = "Noah"}
     ]
     ```
     (Use whatever author info is appropriate — check git config or existing files for the correct name)
   - Add classifiers:
     ```toml
     classifiers = [
         "Development Status :: 3 - Alpha",
         "Intended Audience :: Science/Research",
         "License :: OSI Approved :: MIT License",
         "Programming Language :: Python :: 3",
         "Programming Language :: Python :: 3.11",
         "Programming Language :: Python :: 3.12",
         "Programming Language :: Python :: 3.13",
         "Topic :: Scientific/Engineering :: Chemistry",
         "Topic :: Scientific/Engineering :: Physics",
         "Typing :: Typed",
     ]
     ```
   - Add project URLs:
     ```toml
     [project.urls]
     Repository = "https://github.com/noahbkim/diffraction"
     ```
     (Check git remote for correct URL: `git remote get-url origin`)

2. **Create `src/diffraction/py.typed`:**
   - Create an empty file at `src/diffraction/py.typed`
   - This is the PEP 561 marker indicating the package ships inline types
   - Ensure it's included in the package build (hatchling includes all files by default)

3. **Verify hatchling includes py.typed:** Check pyproject.toml for any explicit `[tool.hatch.build]` include/exclude rules. If there are exclude rules, ensure `py.typed` is not excluded.
  </action>
  <verify>
    <automated>uv run python -c "import importlib.resources; ref = importlib.resources.files('diffraction').joinpath('py.typed'); print('py.typed exists:', ref.is_file())" && uv run ruff check pyproject.toml</automated>
  </verify>
  <done>
    - pyproject.toml version is "0.2"
    - pyproject.toml has license, authors, classifiers, readme, project.urls
    - src/diffraction/py.typed exists as empty file
    - py.typed accessible via importlib.resources
  </done>
</task>

<task type="auto">
  <name>Task 2: Deduplicate test constants and close m6</name>
  <files>
    pyproject.toml
    tests/unit/test_lattice.py
    tests/unit/test_crystal.py
    tests/conftest.py
  </files>
  <action>
**Test constant dedup (m14):**

1. **Add `"tests"` to pytest pythonpath** in pyproject.toml:
   - Currently: `pythonpath = ["src"]`
   - Change to: `pythonpath = ["src", "tests"]`
   - This allows `from conftest import CALCITE_LATTICE_PARAMS` in test files

2. **Verify `CALCITE_LATTICE_PARAMS` exists in `tests/conftest.py`:**
   - It should be at line ~26 (already created in Phase 2)
   - Confirm the value matches what's in test_lattice.py and test_crystal.py

3. **Update `tests/unit/test_lattice.py`:**
   - Remove the local `CALCITE_LATTICE_PARAMS` definition (line ~44 and its comment)
   - Add import: `from conftest import CALCITE_LATTICE_PARAMS`
   - Verify all references to `CALCITE_LATTICE_PARAMS` in the file still resolve

4. **Update `tests/unit/test_crystal.py`:**
   - Remove the local `CALCITE_LATTICE_PARAMS` definition (line ~16 and its comment)
   - Add import: `from conftest import CALCITE_LATTICE_PARAMS`
   - Verify all references still resolve

5. **Check for other duplicated constants:** Grep for any other constants defined in both conftest.py and test files. If found, deduplicate them too (import from conftest).

**Close m6 (get_cif_data overloads):**

6. **Document in `src/diffraction/cif/helpers.py`** at the `get_cif_data` function docstring:
   - Add a note to the existing docstring:
     ```
     Note:
         This function returns a wide union type because the return type
         depends on which data names are in NUMERICAL_DATA_NAMES, not on
         argument types. For fully typed numerical lookups, use
         :func:`get_numerical_cif_data` instead.
     ```
   - This formally closes m6 by documenting why @overload is not feasible and pointing to the typed alternative.

7. **Run full test suite to verify nothing broke:**
   `uv run pytest`
  </action>
  <verify>
    <automated>uv run pytest -x -v && uv run mypy --strict src/ tests/</automated>
  </verify>
  <done>
    - CALCITE_LATTICE_PARAMS imported from conftest in both test_lattice.py and test_crystal.py
    - No duplicate CALCITE_LATTICE_PARAMS definitions remain in unit test files
    - pythonpath includes "tests" for conftest imports
    - get_cif_data docstring documents why @overload is not used and points to get_numerical_cif_data
    - All tests pass, mypy --strict passes on entire codebase
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the FULL verification checklist from CONTEXT.md:
1. `uv run pytest` passes
2. `uv run mypy --strict src/ tests/` passes with zero errors
3. `uv run ruff check .` passes
4. `uv run ruff format --check .` passes
5. All REVIEW.md findings addressed (except m16/README, deferred to Phase 8)
6. No Phase 3 deliverables remaining
7. Vector operations no longer silently degrade (Plan 01)
8. `PointGroup("xyz")` gives helpful error, not bare KeyError (Plan 02)
9. `from diffraction import DiffractionError` works (Plan 02)
10. `grep -rn "CALCITE_LATTICE_PARAMS" tests/unit/` shows only imports, no redefinitions
</verification>

<success_criteria>
- pyproject.toml has complete project metadata (license, authors, classifiers, urls, readme)
- Version bumped to 0.2
- py.typed marker exists
- Test constants deduplicated (imported from conftest, not redefined)
- m6 formally closed via documentation
- Full verification checklist passes
- Phase 02.1.1 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/02.1.1-address-review-md/02.1.1-04-SUMMARY.md`
</output>
